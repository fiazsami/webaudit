{
  "name": "cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "webaudit": "./dist/main.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "audit": "node ./dist/main.js audit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "core": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
